# Shared dependencies for both PCs.
# The robot PC only strictly needs: eclipse-zenoh, opencv-python
# The operator PC needs all of them.
eclipse-zenoh>=1.0.0
opencv-python>=4.8.0
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
pygame>=2.5.0
